# API. Bind to all interfaces when this service runs on a private container network.
BIND_ADDR=0.0.0.0:3001

# PostgreSQL. Matches compose.yaml.
DATABASE_URL=postgres://app:app@127.0.0.1:5432/app
DATABASE_MAX_CONNECTIONS=5

# Firebase Admin Auth
FIREBASE_PROJECT_ID=your-firebase-project-id

# Production: paste the complete service-account JSON as one server-only value.
# Prefer setting this through your deployment secret manager, not a checked-in file.
# In a local .env file, wrap the compact JSON in single quotes so dotenv keeps it intact.
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'
FIREBASE_SERVICE_ACCOUNT_JSON=

# Firebase Storage bucket used only by the backend service. The browser must
# never receive object keys or direct Storage access.
FIREBASE_STORAGE_BUCKET=your-firebase-project-id.appspot.com

# The local startup script sets this automatically. It intentionally omits the protocol.
# FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
# FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9199

# OpenAI is optional locally: without a key, captures remain available and
# extraction returns a retryable AI_UNAVAILABLE state. Never set this in a
# browser environment variable.
OPENAI_API_KEY=
# `responses` is the default OpenAI mode. It supports text and PDF extraction
# through the Responses and Files APIs. Use `chat_completions` for providers
# such as DeepSeek; that mode supports text extraction and plan generation,
# but not PDF extraction. Before switching an existing deployment, first let
# the Responses-mode cleanup worker drain any pending provider-file deletions.
OPENAI_API_MODE=responses
# Defaults to OpenAI. The configured mode determines which API endpoints the
# provider must support.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-5.6-terra
