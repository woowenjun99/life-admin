export default function HomePage() {
  return (
    <main>
      <p className="eyebrow">Full-stack starter</p>
      <h1>Next.js, Firebase, and Axum are ready to connect.</h1>
      <p>
        Add your Firebase Web App values to <code>.env.local</code>, then build
        your first authenticated feature through the relative <code>/api</code>{" "}
        proxy.
      </p>
    </main>
  );
}
